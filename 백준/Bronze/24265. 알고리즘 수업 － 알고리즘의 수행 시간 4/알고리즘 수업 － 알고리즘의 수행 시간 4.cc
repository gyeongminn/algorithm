#include <bits/stdc++.h>
#define fastio cin.tie(0)->sync_with_stdio(0)
#define endl '\n'
using namespace std;

int main(void) {
    fastio;
    int n;
    cin >> n;
    cout << 1L * n * (n-1) / 2 << endl << 2;
}