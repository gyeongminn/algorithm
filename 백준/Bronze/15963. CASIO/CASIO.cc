#include <bits/stdc++.h>

#define fastio cin.tie(0)->sync_with_stdio(0);
#define ll long long

using namespace std;

int main() {
    fastio;
    ll a, b;
    cin >> a >> b;
    if (a == b) {
        cout << 1;
    } else {
        cout << 0;
    }
    return 0;
}